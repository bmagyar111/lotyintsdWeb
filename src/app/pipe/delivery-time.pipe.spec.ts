import { DeliveryTimePipe } from './delivery-time.pipe';

describe('DeliveryTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryTimePipe();
    expect(pipe).toBeTruthy();
  });
});
